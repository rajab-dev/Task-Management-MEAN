import { CustomDialogFormDirective } from './custom-dialog-form.directive';

describe('CustomDialogFormDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDialogFormDirective();
    expect(directive).toBeTruthy();
  });
});
